#!/bin/bash
# a2tp-dkms prerm: tear down every dkms-built copy of the module
set -e
if [ "$1" = remove ] || [ "$1" = upgrade ] || [ "$1" = disappear ]; then
	/usr/sbin/dkms remove -m a2tp -v @VER@ --all >/dev/null 2>&1 || true
fi
exit 0
